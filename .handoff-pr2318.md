# Handoff: Structural Review of PR #2318 (Incomplete)

## Status
Analysis infrastructure prepared. File reading and review synthesis NOT started.

## What's Done
1. Loaded `structural-pr-review` skill from `/home/alex/.pi/agent/skills/structural-pr-review/SKILL.md`.
2. Located PR #2318 on Gitea (`https://git.terraphim.cloud/terraphim/terraphim-ai/pulls/2318`).
3. Fetched PR metadata into `/tmp/pr_2318.json`.
4. Fetched full diff (1252 lines) into `/tmp/pr_2318.diff`.
5. Fetched PR-head file contents for all 7 changed files via Gitea API:
   - `/tmp/pr2318_lib.rs` (85 KB)
   - `/tmp/pr2318_pr_gate_result.rs` (18 KB, NEW)
   - `/tmp/pr2318_pr_handlers_impl.rs` (34 KB)
   - `/tmp/pr2318_reconcile_impl.rs` (72 KB)
   - `/tmp/pr2318_spawn_impl.rs` (36 KB)
   - `/tmp/pr2318_extractor_legacy.rs` (5 KB, NEW)
   - `/tmp/pr2318_pr_review_legacy_extractor.rs` (5 KB, NEW)
6. Reconstructed base versions of the 4 modified `.rs` files by reverse-applying the diff in a temp Git repo (`/tmp/pr2318_reconstruct`). Copied to:
   - `/tmp/base_lib.rs`
   - `/tmp/base_pr_handlers_impl.rs`
   - `/tmp/base_reconcile_impl.rs`
   - `/tmp/base_spawn_impl.rs`

## What's Remaining
- **Read every changed file in full** (base + head for modified, head for new).
- **Check cross-file consistency** — types, imports, shared utilities, API contracts.
- **Verify PR claims** against actual code (e.g., `ManagedAgent` `gate_meta`, `OutputPoster::post_raw_as_agent_for_project`, exit-code fallback).
- **Check unchanged context** — functions called by new code for pre-existing bugs.
- **Apply custom rules** from `.claude/review-rules/` or `CLAUDE.md` if any exist.
- **Select diagram type** — likely `sequenceDiagram` (multi-component: orchestrator → gate agents → status poster).
- **Classify findings** — P0/P1/P2 severity.
- **Calculate confidence score** (1–5).
- **Produce structured output** following the exact skill template (Summary, Confidence Score, Important Files Changed, Diagram, Inline Findings, Comments Outside Diff, Footer).
- **Post review to Gitea** using `gtr comment --owner terraphim --repo terraphim-ai --index 2318 --body-file /tmp/review.md`.

## Next-Agent Starting Position
All required inputs are cached in `/tmp`:
- Diff: `/tmp/pr_2318.diff`
- PR metadata: `/tmp/pr_2318.json`
- PR head files: `/tmp/pr2318_*.rs`
- Base files: `/tmp/base_*.rs`

Start by reading the new module `/tmp/pr2318_pr_gate_result.rs` and the modified orchestrator files, then proceed through the review dimensions in the skill.

## PR Quick Reference
- **Title**: Fix #2301: add PrGateResult contract for PR fan-out gates
- **Author**: root
- **Head**: `073ede3a3d0f87ecd7843386d6284e2c9573f4cb`
- **Base**: `9424eef72129d56636737b7d3d08564948651f90` (main)
- **Size**: +1123 / −11, 7 files changed
