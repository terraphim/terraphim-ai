# Trait System

Rust's mechanism for shared behaviour: defining interfaces, default methods, associated types, and dynamic dispatch.

synonyms:: trait, impl Trait, dyn Trait, trait bounds, associated type, trait object, Send, Sync, marker trait
related:: Arc, async/await, generics
context:: types
cost:: low
