# Ownership and Borrowing

Rust's memory safety model: each value has one owner, references obey borrowing rules enforced at compile time.

synonyms:: ownership, borrow checker, lifetime, reference, borrowing, mutable reference, zero-copy, unsafe, memory safety
related:: Arc, trait, concurrency
context:: safety
cost:: low
