# Docker Compose

Multi-container orchestration for local development and testing. V2 runs as `docker compose` (no hyphen).

synonyms:: docker compose, docker-compose, compose.yaml, docker-compose up, docker compose build, container orchestration
related:: docker build, ci-cd pipeline, caddy
context:: build
cost:: medium
