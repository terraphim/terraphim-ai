# Caddy

Web server with automatic HTTPS via ACME. Used as reverse proxy, static file server, and TLS terminator.

synonyms:: caddy, caddyfile, reverse proxy, HTTPS, ACME, caddy server, caddy reload
related:: docker compose, infrastructure, monitoring
context:: networking
cost:: low
