# GitHub Actions

YAML-based CI/CD platform integrated with GitHub repositories. Workflows live in `.github/workflows/`.

synonyms:: GitHub Actions, GHA, actions workflow, workflow dispatch, matrix strategy, concurrency group
related:: pipeline, runner, artifact, stage, job
context:: ci
cost:: low
