---
id: 372f40d3655e418a97af603c85317d6e-1780667954822
command: echo "Cannot read GitHub workflows"
exit_code: 1
source: Project
captured_at: 2026-06-05T13:59:14.822080906+00:00
working_dir: /home/alex/projects/terraphim/terraphim-ai
failing_subcommand: echo "Cannot read GitHub workflows"
tags:
  - learning
  - exit-1
entities:
  - graph-embeddings-learnings
  - service
importance_total: 0.3800
importance_severity: 0.3000
importance_repetition: 3
importance_recency: 1.0000
importance_has_correction: false
---

## Command

`echo "Cannot read GitHub workflows"`

### Full Chain

`cat .github/workflows/*.yml 2>/dev/null | head -100 || echo "No GitHub workflows found locally"
gh api repos/terraphim/terraphim-ai/contents/.github/workflows --jq '.[].name' 2>/dev/null || echo "Cannot read GitHub workflows"`

## Error Output

```
name: ADF Agent Validation

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      config_path:
        description: "Path to orchestrator config file"
        required: false
        default: "/opt/ai-dark-factory/orchestrator.toml"
        type: string
      skip_model_probe:
        description: "Skip model availability probe (faster)"
        required: false
        default: "false"
        type: boolean

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10

jobs:
  agent-validation:
    name: Validate All ADF Agents
    runs-on: [self-hosted, bigbox]
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-action@stable
        with:
          components: rustfmt, clippy

      - name: Build adf binary
        run: |
          cargo build --release -p terraphim_orchestrator --bin adf

      - name: Run agent validation (all modes)
        id: validate
        run: |
          CONFIG_PATH="${{ inputs.config_path || '/opt/ai-dark-factory/orchestrator.toml' }}"
          SKIP_MODEL=""
          if [ "${{ inputs.skip_model_probe }}" = "true" ]; then
            SKIP_MODEL="--skip-model-probe"
          fi

          if [ -f "$CONFIG_PATH" ]; then
            ./target/release/adf agent validate-all --config "$CONFIG_PATH" --format json $SKIP_MODEL > adf-validation-report.json 2>&1
            EXIT_CODE=$?
            echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
            echo "report_file=adf-validation-report.json" >> $GITHUB_OUTPUT
          else
            echo "config_file_not_found=true" >> $GITHUB_OUTPUT
            echo "exit_code=1" >> $GITHUB_OUTPUT
            echo "WARNING: Config file $CONFIG_PATH not found, skipping validation" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload validation report
        if: env.exit_code != '1' || env.config_file_not_found != 'true'
        uses: a
```

