---
id: ac0719f19be04d7081cca6e70c8dbd83-1779317298957
command: git diff -- crates/terraphim_orchestrator/src/config.rs crates/terraphim_orchestrator/src/project_adf.rs crates/terraphim_orchestrator/src/bin/adf.rs crates/terraphim_orchestrator/tests/adf_check_tests.rs crates/terraphim_orchestrator/tests/project_source_tests.rs
exit_code: 1
source: Project
captured_at: 2026-05-20T22:48:18.957989365+00:00
working_dir: /home/alex/projects/terraphim/terraphim-ai
tags:
  - learning
  - exit-1
entities:
  - database
  - configuration
importance_total: 0.4100
importance_severity: 0.3000
importance_repetition: 4
importance_recency: 1.0000
importance_has_correction: false
---

## Command

`git diff -- crates/terraphim_orchestrator/src/config.rs crates/terraphim_orchestrator/src/project_adf.rs crates/terraphim_orchestrator/src/bin/adf.rs crates/terraphim_orchestrator/tests/adf_check_tests.rs crates/terraphim_orchestrator/tests/project_source_tests.rs`

## Error Output

```
diff --git a/crates/terraphim_orchestrator/src/bin/adf.rs b/crates/terraphim_orchestrator/src/bin/adf.rs
index d81142267..a5768a89d 100644
--- a/crates/terraphim_orchestrator/src/bin/adf.rs
+++ b/crates/terraphim_orchestrator/src/bin/adf.rs
@@ -261,6 +261,7 @@ fn run_local_check(cwd: PathBuf) -> ExitCode {
         quickwit: None,
         projects: vec![project],
         include: vec![],
+        project_sources: vec![],
         providers: vec![],
         provider_budget_state_file: None,
         pause_dir: None,
@@ -376,6 +377,7 @@ async fn run_local_agent(agent_name: &str, cwd: PathBuf) -> ExitCode {
         quickwit: None,
         projects: vec![project],
         include: vec![],
+        project_sources: vec![],
         providers: vec![],
         provider_budget_state_file: None,
         pause_dir: None,
diff --git a/crates/terraphim_orchestrator/src/config.rs b/crates/terraphim_orchestrator/src/config.rs
index db469938b..fd117bc15 100644
--- a/crates/terraphim_orchestrator/src/config.rs
+++ b/crates/terraphim_orchestrator/src/config.rs
@@ -73,6 +73,24 @@ pub struct Project {
     pub max_concurrent_mention_agents: Option<usize>,
 }
 
+/// Global registry entry for loading a repo-local `.terraphim/adf.toml`.
+#[derive(Debug, Clone, Serialize, Deserialize)]
+pub struct ProjectSourceConfig {
+    /// Project id expected inside the project-local config.
+    pub id: String,
+    /// Repository/project root.
+    pub root: PathBuf,
+    /// Config path relative to `root`, or absolute for operator-managed paths.
+    pub config: PathBuf,
+    /// Disabled sources are retained in the loaded config but not merged.
+    #[serde(default = "default_project_source_enabled")]
+    pub enabled: bool,
+}
+
+fn default_project_source_enabled() -> bool {
+    true
+}
+
 /// Top-level orchestrator configuration (parsed from TOML).
 #[derive(Debug, Clone, Serialize, Deserialize)]
 pub struct OrchestratorConfig {
@@ -152,6 +170,9 @@ pub struct OrchestratorConfig {
     
```

