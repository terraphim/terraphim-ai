---
id: 3eba7a7508384aecbee857cc16966e3e-1779747062889
command: grep -n 'LocalAgent\|local.*agent\|--agent\|--local\|fn main\|match\|Cli::' /home/alex/projects/terraphim/terraphim-ai/crates/terraphim_orchestrator/src/bin/adf.rs | head -30
exit_code: 1
source: Project
captured_at: 2026-05-25T22:11:02.889697497+00:00
working_dir: /home/alex/projects/terraphim/terraphim-ai
tags:
  - learning
  - exit-1
importance_total: 0.3200
importance_severity: 0.3000
importance_repetition: 1
importance_recency: 1.0000
importance_has_correction: false
---

## Command

`grep -n 'LocalAgent\|local.*agent\|--agent\|--local\|fn main\|match\|Cli::' /home/alex/projects/terraphim/terraphim-ai/crates/terraphim_orchestrator/src/bin/adf.rs | head -30`

## Error Output

```
17:/// These providers are matched against task prompts via KeywordRouter.
100:    LocalAgent { agent_name: String },
114:        match arg.as_str() {
121:            "--local" => {
123:                match next.as_deref() {
127:                    Some("--agent") => {
130:                            .ok_or_else(|| "--local --agent requires an agent name".to_string())?;
131:                        local_mode = Some(agent_name.clone());
135:                            "--local must be followed by --check or --agent (use: adf --local --check or adf --local --agent NAME)"
141:            "-h" | "--help" => return Ok(Cli::Help),
142:            "-V" | "--version" => return Ok(Cli::Version),
145:                return Ok(Cli::Agent { sub_args });
160:        Ok(Cli::LocalCheck)
162:        Ok(Cli::LocalAgent {
166:        Ok(Cli::Check { config })
170:        Ok(Cli::Run { config })
180:    println!("    adf --local --check         Discover .terraphim/adf.toml and validate");
181:    println!("    adf --local --agent NAME    Run named agent from .terraphim/adf.toml locally");
196:    let subcommand = match parse_agent_args(&sub_args) {
204:    match subcommand {
211:            let adf_config = match terraphim_orchestrator::ProjectAdfConfig::discover_and_load(
228:            let (project_def, agents) = match (&adf_config).try_into() {
315:            let adf_config = match terraphim_orchestrator::ProjectAdfConfig::discover_and_load(
331:            let (project_def, agents) = match (&adf_config).try_into() {
413:    let config = match OrchestratorConfig::from_file(&path) {
431:    let adf_config = match terraphim_orchestrator::ProjectAdfConfig::discover_and_load(&cwd) {
435:                "adf --local --check: no .terraphim/adf.toml found at or above {}",
441:            eprintln!("adf --local --check FAILED: {e}");
453:    let (project, agents) = match (&adf_config).try_into() {
456:            eprintln!("adf --local --check FAILED conversion: {e}");

```

