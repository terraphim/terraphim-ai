---
id: 47cb9995bb274c59884ee2c929502da1-1779355260765
command: ssh bigbox 'ts=$(date +%Y%m%d-%H%M); sudo cp /opt/ai-dark-factory/conf.d/gitea.toml /opt/ai-dark-factory/conf.d/gitea.toml.bak-timeout-smoke-$ts; sudo tee -a /opt/ai-dark-factory/conf.d/gitea.toml >/dev/null <<'"'"'EOF'"'"'

[[agents]]
name = "gitea-timeout-smoke"
project = "gitea"
layer = "Core"
cli_tool = "/home/alex/.bun/bin/opencode"
provider = "kimi-for-coding"
model = "k2p5"
capabilities = ["code"]
max_cpu_seconds = 5
grace_period_secs = 1
gitea_issue = 52
task = "This is an ADF timeout smoke test. Do not modify files. Wait until the orchestrator wall-clock timeout terminates this process."
EOF
GITEA_TOKEN=dummy /usr/local/bin/adf --check /opt/ai-dark-factory/orchestrator.toml >/tmp/adf-timeout-smoke-check.log
exit_code: 1
source: Project
captured_at: 2026-05-21T09:21:00.765131153+00:00
working_dir: /home/alex/projects/terraphim/terraphim-ai
failing_subcommand: ssh bigbox 'ts=$(date +%Y%m%d-%H%M); sudo cp /opt/ai-dark-factory/conf.d/gitea.toml /opt/ai-dark-factory/conf.d/gitea.toml.bak-timeout-smoke-$ts; sudo tee -a /opt/ai-dark-factory/conf.d/gitea.toml >/dev/null <<'"'"'EOF'"'"'

[[agents]]
name = "gitea-timeout-smoke"
project = "gitea"
layer = "Core"
cli_tool = "/home/alex/.bun/bin/opencode"
provider = "kimi-for-coding"
model = "k2p5"
capabilities = ["code"]
max_cpu_seconds = 5
grace_period_secs = 1
gitea_issue = 52
task = "This is an ADF timeout smoke test. Do not modify files. Wait until the orchestrator wall-clock timeout terminates this process."
EOF
GITEA_TOKEN=dummy /usr/local/bin/adf --check /opt/ai-dark-factory/orchestrator.toml >/tmp/adf-timeout-smoke-check.log
tags:
  - learning
  - exit-1
entities:
  - bun
  - graph-embeddings-learnings
  - terraphim_ai
importance_total: 0.5900
importance_severity: 0.3000
importance_repetition: 17
importance_recency: 1.0000
importance_has_correction: false
---

## Command

`ssh bigbox 'ts=$(date +%Y%m%d-%H%M); sudo cp /opt/ai-dark-factory/conf.d/gitea.toml /opt/ai-dark-factory/conf.d/gitea.toml.bak-timeout-smoke-$ts; sudo tee -a /opt/ai-dark-factory/conf.d/gitea.toml >/dev/null <<'"'"'EOF'"'"'

[[agents]]
name = "gitea-timeout-smoke"
project = "gitea"
layer = "Core"
cli_tool = "/home/alex/.bun/bin/opencode"
provider = "kimi-for-coding"
model = "k2p5"
capabilities = ["code"]
max_cpu_seconds = 5
grace_period_secs = 1
gitea_issue = 52
task = "This is an ADF timeout smoke test. Do not modify files. Wait until the orchestrator wall-clock timeout terminates this process."
EOF
GITEA_TOKEN=dummy /usr/local/bin/adf --check /opt/ai-dark-factory/orchestrator.toml >/tmp/adf-timeout-smoke-check.log`

### Full Chain

`ssh bigbox 'ts=$(date +%Y%m%d-%H%M); sudo cp /opt/ai-dark-factory/conf.d/gitea.toml /opt/ai-dark-factory/conf.d/gitea.toml.bak-timeout-smoke-$ts; sudo tee -a /opt/ai-dark-factory/conf.d/gitea.toml >/dev/null <<'"'"'EOF'"'"'

[[agents]]
name = "gitea-timeout-smoke"
project = "gitea"
layer = "Core"
cli_tool = "/home/alex/.bun/bin/opencode"
provider = "kimi-for-coding"
model = "k2p5"
capabilities = ["code"]
max_cpu_seconds = 5
grace_period_secs = 1
gitea_issue = 52
task = "This is an ADF timeout smoke test. Do not modify files. Wait until the orchestrator wall-clock timeout terminates this process."
EOF
GITEA_TOKEN=dummy /usr/local/bin/adf --check /opt/ai-dark-factory/orchestrator.toml >/tmp/adf-timeout-smoke-check.log && sudo systemctl restart adf-orchestrator && systemctl is-active adf-orchestrator && printf "%s\n" "$ts"'`

## Error Output

```
adf --check FAILED validation for /opt/ai-dark-factory/orchestrator.toml: banned LLM provider 'k2p5' in model for agent 'gitea-timeout-smoke' (allowed: claude-code, opencode-go, kimi-for-coding, minimax-coding-plan, zai-coding-plan)

```

