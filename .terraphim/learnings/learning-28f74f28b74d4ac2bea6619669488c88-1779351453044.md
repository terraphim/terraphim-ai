---
id: 28f74f28b74d4ac2bea6619669488c88-1779351453044
command: ssh bigbox 'python3 - <<'"'"'PY'"'"'
import tomllib
from pathlib import Path
for path in [Path("/opt/ai-dark-factory/orchestrator.toml"), *Path("/opt/ai-dark-factory/conf.d").glob("*.toml")]:
    try:
        data = tomllib.loads(path.read_text())
    except Exception as e:
        print(f"{path}: parse_error {e}")
        continue
    for p in data.get("projects", []):
        if p.get("id") == "gitea":
            g = p.get("gitea", {})
            print(f"file={path}")
            print(f"id={p.get('id')}")
            print(f"working_dir={p.get('working_dir')}")
            print(f"max_concurrent_agents={p.get('max_concurrent_agents')}")
            print(f"gitea_base_url={g.get('base_url')}")
            print(f"gitea_owner={g.get('owner')}")
            print(f"gitea_repo={g.get('repo')}")
            print(f"token_present={bool(g.get('token'))}")
            print(f"mentions={p.get('mentions', {})}")
PY'
exit_code: 1
source: Project
captured_at: 2026-05-21T08:17:33.044197976+00:00
working_dir: /home/alex/projects/terraphim/terraphim-ai
tags:
  - learning
  - exit-1
importance_total: 0.5900
importance_severity: 0.3000
importance_repetition: 16
importance_recency: 1.0000
importance_has_correction: false
---

## Command

`ssh bigbox 'python3 - <<'"'"'PY'"'"'
import tomllib
from pathlib import Path
for path in [Path("/opt/ai-dark-factory/orchestrator.toml"), *Path("/opt/ai-dark-factory/conf.d").glob("*.toml")]:
    try:
        data = tomllib.loads(path.read_text())
    except Exception as e:
        print(f"{path}: parse_error {e}")
        continue
    for p in data.get("projects", []):
        if p.get("id") == "gitea":
            g = p.get("gitea", {})
            print(f"file={path}")
            print(f"id={p.get('id')}")
            print(f"working_dir={p.get('working_dir')}")
            print(f"max_concurrent_agents={p.get('max_concurrent_agents')}")
            print(f"gitea_base_url={g.get('base_url')}")
            print(f"gitea_owner={g.get('owner')}")
            print(f"gitea_repo={g.get('repo')}")
            print(f"token_present={bool(g.get('token'))}")
            print(f"mentions={p.get('mentions', {})}")
PY'`

## Error Output

```
file=/opt/ai-dark-factory/conf.d/gitea.toml
id=None
Traceback (most recent call last):
  File "<stdin>", line 14, in <module>
NameError: name 'working_dir' is not defined

```

