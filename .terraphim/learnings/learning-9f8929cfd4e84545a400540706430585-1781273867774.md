---
id: 9f8929cfd4e84545a400540706430585-1781273867774
command: gtr view-issue --owner terraphim --repo digital-twins --issue 122 2>/dev/null | python3 -c "
import sys, json
i = json.load(sys.stdin)
print(f'Title: {i[\"title\"]}')
print(f'Body: {i.get(\"body\",\"\")[:500]}')
"
exit_code: 1
source: Project
captured_at: 2026-06-12T14:17:47.774309241+00:00
working_dir: /home/alex/projects/terraphim/terraphim-ai
failing_subcommand: gtr view-issue --owner terraphim --repo digital-twins --issue 122 2>/dev/null | python3 -c "
import sys, json
i = json.load(sys.stdin)
print(f'Title: {i[\"title\"]}')
print(f'Body: {i.get(\"body\",\"\")[:500]}')
"
tags:
  - learning
  - exit-1
entities:
  - api
  - issue-tracking
importance_total: 0.4400
importance_severity: 0.3000
importance_repetition: 5
importance_recency: 1.0000
importance_has_correction: false
---

## Command

`gtr view-issue --owner terraphim --repo digital-twins --issue 122 2>/dev/null | python3 -c "
import sys, json
i = json.load(sys.stdin)
print(f'Title: {i[\"title\"]}')
print(f'Body: {i.get(\"body\",\"\")[:500]}')
"`

### Full Chain

`gtr view-issue --owner terraphim --repo digital-twins --issue 122 2>/dev/null | python3 -c "
import sys, json
i = json.load(sys.stdin)
print(f'Title: {i[\"title\"]}')
print(f'Body: {i.get(\"body\",\"\")[:500]}')
" && echo "=== Comments on #121 ===" && curl -s "$GITEA_URL/api/v1/repos/terraphim/digital-twins/issues/121/comments" -H "Authorization: token $GITEA_TOKEN" | python3 -c "
import sys, json
for c in json.load(sys.stdin)[-3:]:
    body = c['body'][:200].replace(chr(10),' ')
    print(f'  #{c[\"id\"]:5d} {c[\"user\"][\"login\"]:12s}: {body}')
"`

## Error Output

```
Title: [ADF] Auto-merge failed for PR #121
Body: AutoMerge handler failed to merge PR #121 on project `digital-twins`.

Head SHA: `[AWS_SECRET_REDACTED]`

Error: API error: Gitea merge_pull error 405 Method Not Allowed on PR 121: {"message":"Please try again later","url":"https://git.terraphim.cloud/api/swagger"}

The PR was left open; a human needs to investigate (merge conflict, protected branch, permissions, transient API failure).

Refs: ROC v1 Step G handler, adf-fleet#35.
=== Comments on #121 ===
  #43108 root        : <h3>Summary</h3>  PR #121 claims to add the `native-design-test-verification` gate entry to `AGENTS.md` as required by issue #115. However, the PR carries **zero changes**: 0 additions, 0 deletions, 0
  #43112 root        : <h3>Summary</h3>  This PR adds a single row to the gate agents table in `AGENTS.md` for the new `native-design-test-verification` gate, and updates the prose count from "Five" to "Six" stateless gate 
  #43123 root        : Superseded by PR #123. The task/108 branch incorporates the AGENTS.md native-design-test-verification entry (commits 346093f + 9fcf4a9) and will land it in main when merged. Closing this PR.

```

