# Container Orchestration

Managing containerised applications at scale including scheduling, scaling, and networking.

synonyms:: Docker, container, docker compose, Kubernetes, k8s, pod, service, ingress, namespace, Dockerfile, image, registry, volume, network, orchestration, container runtime
