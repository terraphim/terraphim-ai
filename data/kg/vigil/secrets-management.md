# Secrets Management

Secure storage, rotation, and access control for sensitive credentials, API keys, and configuration data.

synonyms:: secret, credential, API key, token, password, vault, 1Password, op inject, environment variable, secret rotation, secret scanning, .env, dotenv, key vault
