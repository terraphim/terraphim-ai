# Type System

Rust's expressive type system including generics, traits, enums, and zero-cost abstractions for compile-time safety.

synonyms:: trait, generic, enum, struct, impl, where clause, trait bound, associated type, type alias, newtype, phantom type, marker trait, dyn trait, trait object, vtable, monomorphisation
