# Cargo Ecosystem

The Rust package manager and build system including workspace management, feature flags, and dependency resolution.

synonyms:: cargo, crate, Cargo.toml, workspace, feature flag, dependency, cargo clippy, cargo fmt, cargo test, cargo build, cargo publish, crates.io, cargo tree, cargo audit, cargo watch
