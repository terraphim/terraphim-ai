# Ownership

Rust's ownership system ensures memory safety without garbage collection through move semantics, borrowing, and lifetime annotations.

synonyms:: borrow, borrowing, borrow checker, lifetime, move semantics, clone, Copy trait, Drop trait, reference, mutable reference, shared reference, dangling pointer, use after free, double free
