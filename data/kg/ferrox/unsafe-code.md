# Unsafe Code

Rust's unsafe blocks for operations the borrow checker cannot verify, requiring manual safety guarantees.

synonyms:: unsafe, raw pointer, FFI, foreign function interface, transmute, UB, undefined behaviour, soundness, invariant, Pin, Unpin, Send, Sync, aliasing, miri
