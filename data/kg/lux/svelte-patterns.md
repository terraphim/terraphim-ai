# Svelte Patterns

Svelte-specific patterns for building reactive, compiled frontend applications with minimal runtime overhead.

synonyms:: Svelte, SvelteKit, rune, $state, $derived, $effect, bind, each block, await block, action, use directive, transition, Vite, Tauri, TypeScript, store, writable, readable
