# Responsive Design

Techniques for building interfaces that adapt gracefully across different screen sizes and device types.

synonyms:: responsive, media query, breakpoint, mobile-first, viewport, flexbox, CSS grid, fluid layout, adaptive, progressive enhancement, graceful degradation, touch target, container query
