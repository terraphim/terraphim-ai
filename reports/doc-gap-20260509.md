# Documentation Gap Report: 2026-05-09

**Agent:** Ferrox (Rust Engineer)
**Scope:** `terraphim_types`, `terraphim_config`, `terraphim_automata` crates
**Tool:** `cargo clippy -- -W missing_docs`

## Summary

| Crate | Missing docs before | Missing docs after | Status |
|---|---|---|---|
| `terraphim_types` | 77 | 0 | RESOLVED |
| `terraphim_config` | 38 | 38 | OPEN |
| `terraphim_automata` | 87 | 87 | OPEN |
| `terraphim_rolegraph` | 0 | 0 | OK |
| `terraphim_persistence` | 0 | 0 | OK |

## Resolved This Session

`terraphim_types` now has zero `missing_docs` warnings. Items documented:

- `score/mod.rs`: module `//!`, sub-module item docs, `Scorer`, `Query`, `Similarity`, `ScoreError`, `sort_documents`
- `llm_usage.rs`: all struct fields and methods for `LlmUsage`, `LlmResult`, `ModelPricing`
- `lib.rs`: `DocumentType` variants, `RouteDirective` fields, `MarkdownDirectives` struct and all fields, `Edge` struct-level doc and `Edge::new`, `Thesaurus::keys`, `IndexedDocument` methods, `NormalizedTermValue` methods, `ConversationId`/`MessageId` constructors, `ChatMessage` struct, `ContextHistory::new`, `MultiAgentContext::new`
- `review.rs`: all variants of `FindingSeverity` and `FindingCategory`, all fields of `ReviewFinding` and `ReviewAgentOutput`

Committed: `9f70e0d2c`

## Open Gaps

### `terraphim_config` (38 warnings)

High-priority because configuration types are the public API entry point for all role and haystack configuration. Callers depend on field semantics being documented.

### `terraphim_automata` (87 warnings)

High-priority because this crate exposes a WASM surface and is used from JavaScript. Field and method docs feed TypeScript type definitions via `tsify`.

## Recommended Next Steps

1. Document `terraphim_config` public API (role fields, haystack config fields, `Config` struct methods)
2. Document `terraphim_automata` WASM-exposed types and methods
3. Add `#![warn(missing_docs)]` lint gate to all fully-documented crates to prevent regression

Theme-ID: doc-gap
@adf:reviewer please action this finding.
