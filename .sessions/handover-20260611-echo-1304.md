# Handover: Pre-push --all-features hook (#1304)

**Agent**: Echo (Twin Maintainer)
**Date**: 2026-06-11 22:10 CEST
**Issue**: #1304 — Add pre-push --all-features cargo check developer hook
**PR**: #2480 (open, awaiting @adf:quality-coordinator review)

## Progress Summary

### Completed
- Created `scripts/pre-push-check.sh` — runs `cargo check --workspace --all-features`; exits 0 on pass, 1 on failure; `SKIP_ALL_FEATURES_CHECK=1` escape hatch
- Updated `scripts/install-hooks.sh` — `install_native_hooks()` now copies script to `.git/hooks/pre-push`; hook overview message updated
- Updated `CONTRIBUTING.md` — one-sentence documentation under "Code Quality Tools"
- Committed as single clean commit on `task/1304-pre-push-all-features-check-v2`
- Pushed to Gitea; PR #2480 open; handover comment posted on issue #1304

### Not Done (out of scope)
- Setting the source file executable (DCG blocks permission-setting commands; install-hooks.sh handles this on installation)
- Fixing the pre-existing `terraphim_service` compilation failure that the hook detects

## Current State

**Branch**: `task/1304-pre-push-all-features-check-v2`
**Commit**: `1e70e5614d`
**PR**: https://git.terraphim.cloud/terraphim/terraphim-ai/pulls/2480
**Status**: COMPLETE — awaiting quality review

## Key Technical Notes

1. `cargo check --workspace --all-features` currently FAILS (terraphim_service, pre-existing issue in registry dep). This is expected — the hook correctly detects it. Not introduced by this PR.
2. Source file is 100644 in git (not executable). install-hooks.sh sets permissions on the `.git/hooks/pre-push` copy. This is consistent with how the project installs hooks.
3. Contaminated branch `task/1304-pre-push-all-features-hook` exists locally — it has the right logic but 17 extra artefact commits. Do not push or PR that branch.

## Next Steps for Reviewer

1. Verify three files changed: `scripts/pre-push-check.sh` (new), `scripts/install-hooks.sh` (+9 lines), `CONTRIBUTING.md` (+2 lines)
2. Run `./scripts/install-hooks.sh` and verify "Installed pre-push --all-features check hook" appears
3. Verify `.git/hooks/pre-push` is installed
4. Verify `SKIP_ALL_FEATURES_CHECK=1 git push` bypasses the hook
