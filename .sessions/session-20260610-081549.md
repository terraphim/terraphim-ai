# Session: Runner Health-Check Workflow — Issue #1310

**Agent**: implementation-swarm (Echo)
**Started**: 2026-06-10 08:15 CEST
**Branch**: task/1310-runner-health-check
**Issue**: https://git.terraphim.cloud/terraphim/terraphim-ai/issues/1310

## Task

Add `.gitea/workflows/runner-health.yml`: a scheduled workflow (every 15 minutes) with:
1. A heartbeat job that runs on `terraphim-native`, outputs `echo ok`, and pings `RUNNER_HEALTH_PING_URL` secret
2. A runner-registration job that queries the Gitea API for runner count and fails if zero runners are online

## Context

- Workspace HEAD: 6ba7c8e5ab
- Only `.gitea/workflows/native-ci.yml` exists today (push + workflow_dispatch trigger)
- Gitea API: `GET /api/v1/repos/{owner}/{repo}/actions/runners`

## Subtasks

| # | Task | Size | Status |
|---|------|------|--------|
| 1 | Write `.gitea/workflows/runner-health.yml` with heartbeat job | S | TODO |
| 2 | Add runner-registration check job using Gitea API | S | TODO |
| 3 | Verify YAML syntax with `python3 -c "import yaml; yaml.safe_load(open(...))"` | S | TODO |
| 4 | Commit + push + create PR | S | TODO |

## Decisions

- Using `python3` (available on native runner) to parse JSON, avoiding `jq` dependency uncertainty
- Heartbeat ping is conditional on secret being set (`if: env.RUNNER_HEALTH_PING_URL != ''`)
- Runner registration check uses `GITEA_TOKEN` (already used in CI workflows per CLAUDE.md)
- `runs-on: terraphim-native` matches existing `native-ci.yml` pattern

## Progress

- [x] Session file created
- [ ] Workflow YAML written
- [ ] Syntax verified
- [ ] Committed and PR created
