# Session: Fix #2189 Native Gitea runner structural review (P1 x4, P2 x6)

**Agent**: Echo / implementation-swarm  
**Issue**: terraphim-ai#2189  
**Start**: 2026-06-10 02:15 CEST  
**Branch**: task/2189-gitea-runner-p1-p2-fixes

## Scope (ordered by impact)

1. **P1-4** `task_worker.rs`: compile/policy errors → report FAILURE to Gitea (not silent)
2. **P1-1** `poller.rs`: silent failure loop → exponential backoff + exit after 10 consecutive
3. **P1-3** `task_worker.rs`: checkout fallback → hard fail + FAILURE report
4. **P2-1** `client.rs`: add connect_timeout(10s) + timeout(30s) to reqwest client
5. **P2-2** `state.rs`: log warning instead of `let _ =` on set_permissions failure
6. **P2-3** `policy.rs`: remove `sh`, `bash`, `cd` from ALLOWLIST (shell bypass vector)

*P1-2 (double-fetch) already addressed by Gitea's claim guard per code comment. Skip.*  
*P2-4 (legacy mirror errors) already only emits warn, acceptable. Skip.*  
*P2-5 (lib-only tests) and P2-6 (remote sync) are ops tasks, not code changes. Skip.*

## Key Files
- `crates/terraphim_gitea_runner/src/poller.rs` (P1-1)
- `crates/terraphim_gitea_runner/src/task_worker.rs` (P1-3, P1-4)
- `crates/terraphim_gitea_runner/src/client.rs` (P2-1)
- `crates/terraphim_gitea_runner/src/state.rs` (P2-2)
- `crates/terraphim_gitea_runner/src/policy.rs` (P2-3)
