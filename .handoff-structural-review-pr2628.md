# Incomplete Handoff: Structural Review of PR #2628

**Task:** Structural review of PR #2628 — "Fix #1715: use per-project tracker for PR commit statuses"  
**Project:** terraphim/terraphim-ai  
**Head:** `46900c28ea3a662f951f865cbc34b94dc745a853`  
**Base:** `main @ 7ab758dab1963c2fdfc6d19ad4877ac35de8ffed`  
**Started:** 2026-06-13T11:19+02:00  
**Status:** Partial analysis complete; full review not yet produced.

---

## What has been done

1. **Fetched PR metadata** via `gtr view-pull --owner terraphim --repo terraphim-ai --index 2628`.
   - PR is open, author `root`, 48 changed files, +4603 / -444 LOC (per Gitea), title/description focused on the #1715 per-project commit-status fix.
   - Note: `mergeable: false` in Gitea response — reason not yet investigated.

2. **Obtained diff and file list**:
   - Diff saved to `/tmp/pr2628.diff` (6323 lines).
   - File list saved to `/tmp/pr2628_files.json`.

3. **Cloned PR branch** to a clean working copy:
   - `/tmp/terraphim-ai-pr2628` (branch `task/1715-pr-reviewer-project-source-dispatch-home`).
   - Original repo `/data/projects/terraphim/terraphim-ai` has many corrupt local refs (`bad object refs/heads/...`) that block `git fetch`; the next agent should either continue from `/tmp/terraphim-ai-pr2628` or run a ref cleanup before fetching in the original repo.

4. **Read and partially analyzed key files**:
   - `crates/terraphim_orchestrator/src/webhook.rs` — webhook dispatch, project derived from `repository.full_name`, group aliases, push/PR event handling.
   - `crates/terraphim_orchestrator/src/output_poster.rs` — `OutputPoster::tracker_for(project, agent_name)` already supports per-project/per-agent trackers.
   - `crates/terraphim_orchestrator/src/lib.rs` — located the fix:
     - `post_pending_status` now calls `p.tracker_for(project, agent_name)`.
     - `post_terminal_commit_status` now calls `p.tracker_for(project, agent_name)`.
     - Callers in `handle_review_pr` and agent-exit handling pass the agent's project.
   - `crates/terraphim_orchestrator/src/config.rs` — reviewed `ProjectSourceConfig`, `OrchestratorConfig::project_sources`, `merge_project_sources`, `agents_on_pr_open_for_project`, `IncludeFragment` aggregation.
   - `crates/terraphim_orchestrator/src/project_adf.rs` — not yet read (queued user message interrupted).

5. **Identified PR scope surprise**: the title/description suggest a narrow bug fix, but the branch contains a much larger feature batch including:
   - New `.docs/` design/research documents (12 files).
   - New `project_sources` / repo-local ADF config loader.
   - New `crates/terraphim_orchestrator/src/agent_registry.rs`.
   - New tests: `project_source_tests.rs`, `adf_check_tests.rs`, `tla_validation_tests.rs`.
   - New TLA+ specs under `crates/terraphim_orchestrator/tla/`.
   - Changes to telemetry, dual_mode, error, provider_probe, spawner, agent tests, etc.

---

## What remains to be done

1. **Read remaining changed code files in full** (not just diff hunks):
   - `crates/terraphim_orchestrator/src/agent_registry.rs`
   - `crates/terraphim_orchestrator/src/bin/adf.rs`
   - `crates/terraphim_orchestrator/src/concurrency.rs`
   - `crates/terraphim_orchestrator/src/control_plane/telemetry.rs`
   - `crates/terraphim_orchestrator/src/dual_mode.rs`
   - `crates/terraphim_orchestrator/src/error.rs`
   - `crates/terraphim_orchestrator/src/provider_probe.rs`
   - `crates/terraphim_orchestrator/src/agent_run_record.rs`
   - `crates/terraphim_agent/tests/comprehensive_cli_tests.rs`
   - `crates/terraphim_agent/tests/persistence_tests.rs`
   - `crates/terraphim_config/src/project.rs`
   - `crates/terraphim_spawner/src/config.rs`, `lib.rs`, `output.rs`
   - `.terraphim/adf.toml`
   - New test files under `crates/terraphim_orchestrator/tests/`
   - New TLA+ specs and README.

2. **Read full `lib.rs`** in chunks to understand the broader orchestrator changes beyond the status-fix lines (529 additions / 278 deletions is large; only the status functions and surrounding PR dispatch have been inspected).

3. **Cross-file consistency checks**:
   - Verify `OutputPoster::tracker_for` semantics match all callers.
   - Verify `project_sources` merge logic does not duplicate or skip validation.
   - Verify test coverage for the per-project status fix.
   - Check Cargo.toml / Cargo.lock changes for new dependencies.

4. **Apply custom rules / project conventions** from `.claude/review-rules/` or `AGENTS.md` if present.

5. **Produce the structured review** per `/home/alex/.pi/agent/skills/structural-pr-review/SKILL.md`:
   - Summary
   - Confidence score (1–5)
   - Important files table
   - Mermaid diagram
   - Inline findings (P0/P1/P2)
   - Comments outside diff (if any)
   - Footer with commit hash and review count

6. **Post the review** to Gitea:
   ```bash
   gtr comment --owner terraphim --repo terraphim-ai --issue 2628 --body-file /tmp/review-2628.md
   ```

---

## Recommended next-agent starting position

1. Use the already-cloned branch at `/tmp/terraphim-ai-pr2628` to avoid the corrupt-ref problem in the original repo.
2. Re-read `/tmp/pr2628.diff` and `/tmp/pr2628_files.json` to confirm scope.
3. Continue file reading with the unread files listed above.
4. Run `cargo check -p terraphim_orchestrator` and any relevant tests to validate behaviour.
5. Draft the review in `/tmp/review-2628.md`, then post via `gtr comment`.

---

## Notes / warnings

- **Do not post a partial review.** The current state is analysis-only; no findings have been classified or calibrated.
- The PR is reported as `mergeable: false` by Gitea; consider whether this is due to conflicts, branch protection, or CI status and mention it in the final review if still true.
- The PR bundles a large feature (project-source loader, TLA+ specs, many docs) with the #1715 fix. The final review should treat this as a batch PR and assess each sub-change independently, with the confidence score reflecting the weakest component.
