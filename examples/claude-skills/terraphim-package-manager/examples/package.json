{
  "name": "example-project",
  "version": "1.0.0",
  "description": "Example project demonstrating package manager replacement",
  "scripts": {
    "install": "npm install",
    "dev": "npm run dev",
    "build": "yarn build",
    "test": "pnpm test",
    "lint": "npm run lint",
    "format": "yarn format"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
