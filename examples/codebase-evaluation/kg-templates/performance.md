# Performance Bottleneck

Code sections that cause performance degradation or inefficiency.

synonyms:: slow code, inefficient algorithm, O(n^2) complexity, O(n squared), blocking operation, performance issue, bottleneck, unnecessary allocation, excessive copying, slow query
