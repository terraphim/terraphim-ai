# Security Vulnerability

Security flaws and vulnerabilities that could be exploited.

synonyms:: SQL injection, XSS, cross-site scripting, CSRF, cross-site request forgery, authentication flaw, authorization bypass, insecure deserialization, command injection, path traversal, directory traversal, weak encryption, hardcoded secret, plaintext password
