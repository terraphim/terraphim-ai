# npm install

Install JavaScript/Node.js dependencies. Use bun install instead for faster installs.

synonyms:: npm install, npm i, yarn install, pnpm install
related:: npm run, npm test
transforms:: npm install → bun install
yarn install → bun install
pnpm install → bun install
context:: build
cost:: medium
