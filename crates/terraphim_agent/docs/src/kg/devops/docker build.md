# docker build

Build Docker image from Dockerfile. Use docker compose build for multi-service projects.

synonyms:: docker build, docker buildx build, podman build
related:: docker compose, docker push, docker run
context:: build
cost:: high
