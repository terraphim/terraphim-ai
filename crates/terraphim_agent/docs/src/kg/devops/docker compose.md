# docker compose

Run multi-container Docker applications. Preferred over docker-compose (legacy v1). Use for local development stacks.

synonyms:: docker compose, docker-compose, docker-compose up
docker compose up → docker compose up
context:: build
cost:: medium
