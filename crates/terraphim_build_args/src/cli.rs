/// CLI utilities for build argument management
///
/// This module provides CLI-related functionality.
/// CLI handler
pub struct CliHandler;

impl Default for CliHandler {
    fn default() -> Self {
        Self::new()
    }
}

impl CliHandler {
    pub fn new() -> Self {
        Self
    }
}
