//! Tests for terraphim_egui

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_state_creation() {
        let state = crate::state::AppState::new();
        // Test state creation
    }
}
